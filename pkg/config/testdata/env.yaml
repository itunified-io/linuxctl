version: "1"
kind: Env
metadata:
  name: db01-uat
  domain: example.com
  tags:
    env: uat
    role: db
spec:
  linux:
    $ref: ./linux.yaml
  hypervisor:
    kind: proxmox
  networks:
    mgmt:
      cidr: 10.0.0.0/24
